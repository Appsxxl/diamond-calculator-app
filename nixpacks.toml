[phases.setup]
aptPkgs = ["python3", "python3-dev", "make", "g++", "libsqlite3-dev"]

[phases.install]
cmds = ["npm install -g pnpm@9.12.0", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "pnpm start"
